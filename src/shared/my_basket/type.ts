export default interface MyBasketProps {
  photoURLs: string[]; // Обновим это для массива изображений
  name: string;
  time: string;
  id: number;
  description: string;
  category: string; // Добавляем категорию
}
